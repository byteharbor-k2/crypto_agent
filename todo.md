# TODO

## 核心实现路线

1. **LLMProvider 抽象**
   - 抽象统一的模型调用接口。
   - 支持 Claude 与 Ollama 之间切换。
   - 为后续 B.AI、MLX 等模型来源预留扩展点。

2. **本地模型工具调用稳定性**
   - 验证 Gemma、Qwen 等本地模型在工具调用场景下的表现。
   - 必要时使用 JSON 指令解析替代模型原生 tool calling。
   - 保证本地模型能够稳定完成“理解任务 -> 选择工具 -> 解析结果 -> 继续执行”的 Agent 流程。

3. **支付策略增强**
   - 增加会话预算控制。
   - 增加服务白名单与黑名单。
   - 增加支付审计日志，记录支付金额、服务地址、决策结果和交易凭证。

4. **真实 x402/b402 接入**
   - 在 Mock x402 服务之外，尝试接入真实 x402/b402 服务。
   - 抽象真实支付适配层，与当前模拟支付逻辑区分。
   - 将真实服务接入作为毕业论文中的扩展验证部分。

## 扩展探索

5. **MLX 本地部署**
   - 尝试使用 MLX 在 Apple Silicon 本地部署 Qwen/Gemma 等模型。
   - 与 Ollama、本地模型 API 和远程 API Key 模型进行对比。
   - 论文中可写成“从 API Key 模型迁移到本地模型”的工程优化。

6. **Polymarket / 自动交易 Agent 方向探索**
   - 可作为扩展场景进行调研或原型验证。
   - 不建议作为毕业设计主线，主线仍应围绕“付费访问服务”展开。
   - 交易类 Agent 涉及资金风险、策略风险和合规复杂度，应控制在扩展讨论范围内。
